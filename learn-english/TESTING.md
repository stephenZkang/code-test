# LearnEnglish - 测试验证指南

## 📋 测试清单

本文档提供完整的测试步骤，用于验证 LearnEnglish 应用的所有功能。

---

## 🔧 环境准备

### 前置条件
- ✅ MySQL 运行在 127.0.0.1:3306
- ✅ Java JDK 17+ 已安装
- ✅ Node.js 16+ 已安装
- ✅ Maven 已安装

### 数据库初始化

```bash
# Windows
.\setup-database.bat

# 或手动执行
mysql -u root -proot < database/schema.sql
mysql -u root -proot < database/seed.sql

# 验证数据库
mysql -u root -proot -e "USE learn_english; SELECT COUNT(*) FROM words;"
# 应该显示 100+ 条记录
```

---

## 🔙 后端API测试

### 启动后端服务

```bash
cd learn-english-backend
mvn clean install -DskipTests
mvn spring-boot:run
```

**预期输出**:
```
===========================================
LearnEnglish Backend Started Successfully!
API Documentation: http://localhost:8080
===========================================
```

### API端点测试

#### 1. 单词管理API

```bash
# 获取所有单词
curl http://localhost:8080/api/words

# 获取随机单词
curl "http://localhost:8080/api/words/random?limit=5"

# 获取特定单词
curl http://localhost:8080/api/words/1

# 获取特定分类单词
curl "http://localhost:8080/api/words?category=common"
```

**预期结果**: 返回JSON格式的单词数据，包含 code: 200 和 data 字段

#### 2. 进度追踪API

```bash
# 获取学习进度统计
curl http://localhost:8080/api/progress

# 更新学习进度
curl -X POST http://localhost:8080/api/progress/update \
  -H "Content-Type: application/json" \
  -d '{"wordId":1,"masteryLevel":2,"isBookmarked":false}'

# 获取待复习单词
curl http://localhost:8080/api/progress/due

# 获取收藏单词
curl http://localhost:8080/api/progress/bookmarked
```

**预期结果**: 返回进度统计数据，包括学习天数、掌握单词数等

#### 3. 练习系统API

```bash
# 生成选择题
curl "http://localhost:8080/api/exercises/generate?questionType=MULTIPLE_CHOICE&count=5"

# 生成填空题
curl "http://localhost:8080/api/exercises/generate?questionType=FILL_BLANK&count=5"

# 生成听力题
curl "http://localhost:8080/api/exercises/generate?questionType=LISTENING&count=5"

# 提交练习答案
curl -X POST http://localhost:8080/api/exercises/submit \
  -H "Content-Type: application/json" \
  -d '{"wordId":1,"questionType":"MULTIPLE_CHOICE","userAnswer":"测试","timeSpent":10}'

# 获取练习历史
curl http://localhost:8080/api/exercises/history

# 获取练习统计
curl http://localhost:8080/api/exercises/stats
```

**预期结果**: 返回生成的题目数据，包括选项和正确答案

#### 4. 成就系统API

```bash
# 获取所有成就
curl http://localhost:8080/api/achievements

# 获取用户成就
curl http://localhost:8080/api/achievements/user

# 检查并解锁成就
curl -X POST http://localhost:8080/api/achievements/check
```

**预期结果**: 返回成就列表，包括已解锁和未解锁状态

### 间隔重复算法验证

```bash
# 1. 学习一个新单词
curl -X POST http://localhost:8080/api/progress/update \
  -H "Content-Type: application/json" \
  -d '{"wordId":1,"masteryLevel":0,"isBookmarked":false}'

# 2. 查看进度，检查 nextReviewDate
curl http://localhost:8080/api/progress/history

# 3. 多次更新掌握等级，观察 nextReviewDate 变化
curl -X POST http://localhost:8080/api/progress/update \
  -H "Content-Type: application/json" \
  -d '{"wordId":1,"masteryLevel":1,"isBookmarked":false}'

curl -X POST http://localhost:8080/api/progress/update \
  -H "Content-Type: application/json" \
  -d '{"wordId":1,"masteryLevel":2,"isBookmarked":false}'
```

**预期结果**: 
- masteryLevel 0: 下次复习 1小时后
- masteryLevel 1: 下次复习 4小时后
- masteryLevel 2: 下次复习 1天后

---

## 🎨 前端功能测试

### 启动前端服务

```bash
cd learn-english-frontend
npm install
npm run dev
```

**预期输出**:
```
VITE v5.0.8  ready in XXX ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

### 浏览器测试

访问: http://localhost:3000

#### 1. 首页 - 单词学习

**测试步骤**:
1. ✅ 页面加载显示单词卡片
2. ✅ 点击卡片触发翻转动画
3. ✅ 正面显示英文单词和音标
4. ✅ 背面显示中文和例句
5. ✅ 点击发音按钮播放语音
6. ✅ 点击收藏按钮标记单词
7. ✅ 向左滑动跳过单词
8. ✅ 向右滑动标记为已学习
9. ✅ 显示学习进度 (X/Y)
10. ✅ 单词学完后显示完成提示

**预期结果**: 所有交互流畅，动画平滑，语音播放正常

#### 2. 练习页 - 练习测试

**测试步骤**:
1. ✅ 选择题型 (选择题/填空题/听力题)
2. ✅ 点击"开始练习"生成题目
3. ✅ 选择题: 选择答案并提交
4. ✅ 填空题: 输入答案并提交
5. ✅ 听力题: 播放发音，选择答案
6. ✅ 查看实时反馈 (正确/错误)
7. ✅ 显示正确答案和解释
8. ✅ 点击"下一题"继续
9. ✅ 完成后查看总成绩
10. ✅ 显示正确率统计

**预期结果**: 题目生成正确，答案验证准确，反馈及时

#### 3. 进度页 - 学习统计

**测试步骤**:
1. ✅ 显示连续学习天数
2. ✅ 显示已学习单词数
3. ✅ 显示已掌握单词数
4. ✅ 显示收藏单词数
5. ✅ 显示练习完成数
6. ✅ 显示平均正确率
7. ✅ 显示今日学习单词数
8. ✅ 显示成就徽章 (已解锁/未解锁)
9. ✅ 成就图标和描述正确显示

**预期结果**: 所有统计数据准确，图表显示正常

#### 4. 设置页

**测试步骤**:
1. ✅ 切换深色/浅色模式
2. ✅ 主题切换立即生效
3. ✅ 显示应用版本信息
4. ✅ 显示技术栈信息

**预期结果**: 主题切换流畅，信息显示完整

#### 5. 底部导航

**测试步骤**:
1. ✅ 点击各个Tab切换页面
2. ✅ 当前Tab高亮显示
3. ✅ 页面切换平滑
4. ✅ 导航栏始终固定在底部

**预期结果**: 导航功能正常，高亮状态正确

---

## 📱 移动端响应式测试

### Chrome DevTools 模拟

1. 打开 Chrome DevTools (F12)
2. 点击设备切换工具栏 (Ctrl+Shift+M)
3. 测试不同设备:
   - iPhone 12 Pro (390x844)
   - iPad Air (820x1180)
   - Samsung Galaxy S20 (360x800)
   - Pixel 5 (393x851)

**测试要点**:
- ✅ 布局适配不同屏幕尺寸
- ✅ 触摸目标足够大 (最小44x44px)
- ✅ 文字大小适中，易读
- ✅ 滑动手势流畅
- ✅ 底部导航不遮挡内容
- ✅ 卡片大小合适
- ✅ 按钮间距足够

### 触摸手势测试

在移动设备模拟器中:
1. ✅ 单词卡片左滑
2. ✅ 单词卡片右滑
3. ✅ 卡片点击翻转
4. ✅ 滚动页面流畅
5. ✅ 按钮点击响应快速

---

## 🐳 Docker部署测试

### 构建并启动

```bash
# 确保清理旧容器
docker-compose down -v

# 构建并启动
docker-compose up --build -d
```

### 验证服务

```bash
# 查看容器状态
docker-compose ps

# 预期输出: 所有服务 State 为 Up
# learnenglish-mysql    Up
# learnenglish-backend  Up

# 查看日志
docker-compose logs backend

# 等待后端启动完成后测试
docker-compose logs backend | grep "Started"
```

### 测试容器化的API

```bash
# 测试数据库连接
docker-compose exec mysql mysql -u root -proot -e "SHOW DATABASES;"

# 测试后端API
curl http://localhost:8080/api/words

# 如果失败，检查日志
docker-compose logs backend --tail=50
```

### 清理

```bash
# 停止服务
docker-compose down

# 删除数据卷（谨慎）
# docker-compose down -v
```

---

## ✅ 集成测试场景

### 场景1: 完整学习流程

1. 打开首页，学习10个新单词
2. 每个单词点击翻转查看中文
3. 标记5个为已学习，5个跳过
4. 收藏3个难记的单词
5. 进入进度页查看统计
6. 验证"已学习"数量增加
7. 验证"收藏"数量正确

### 场景2: 练习测试流程

1. 进入练习页
2. 选择"选择题"类型
3. 完成10道题
4. 故意答错几题
5. 查看最终成绩
6. 进入进度页查看练习统计
7. 验证正确率计算准确

### 场景3: 间隔重复验证

1. 学习一个新单词
2. 查看该单词的下次复习时间 (应该是1小时后)
3. 通过API更新掌握等级为2
4. 再次查看下次复习时间 (应该是1天后)
5. 验证算法计算正确

### 场景4: 成就解锁

1. 学习第1个单词，检查是否解锁"First Word"成就
2. 连续3天学习，检查是否解锁"3-Day Streak"成就
3. 完成10道练习，检查是否解锁"Practice Beginner"成就
4. 进入进度页查看所有成就

---

## 🐛 常见问题排查

### 后端无法启动

```bash
# 检查MySQL连接
mysql -u root -proot -e "SELECT 1;"

# 检查端口占用
netstat -ano | findstr :8080

# 查看详细日志
mvn spring-boot:run
```

### 前端无法连接后端

```bash
# 检查后端是否运行
curl http://localhost:8080/api/words

# 检查浏览器控制台错误
# F12 > Console
# F12 > Network

# 检查CORS配置
# CorsConfig.java 应该包含前端地址
```

### 数据库数据错误

```bash
# 重置数据库
mysql -u root -proot -e "DROP DATABASE learn_english;"
mysql -u root -proot < database/schema.sql
mysql -u root -proot < database/seed.sql
```

### Docker构建失败

```bash
# 清理Docker缓存
docker system prune -a

# 重新构建
docker-compose build --no-cache

# 查看详细构建日志
docker-compose up --build
```

---

## 📊 性能测试

### API响应时间

```bash
# 测试单词列表API
time curl http://localhost:8080/api/words
# 预期: < 200ms

# 测试随机单词API
time curl http://localhost:8080/api/words/random?limit=10
# 预期: < 300ms

# 测试进度统计API
time curl http://localhost:8080/api/progress
# 预期: < 500ms
```

### 前端加载时间

1. 打开 Chrome DevTools
2. Network 标签
3. 刷新页面
4. 查看 DOMContentLoaded 和 Load 时间
5. 预期: DOMContentLoaded < 1s, Load < 2s

---

## ✅ 验收标准

### 后端
- ✅ 所有19个API端点正常响应
- ✅ 数据库连接稳定
- ✅ 间隔重复算法计算正确
- ✅ 成就系统正常工作
- ✅ Docker部署成功

### 前端
- ✅ 4个页面全部可访问
- ✅ 所有交互功能正常
- ✅ 响应式布局适配不同设备
- ✅ 加载和错误状态正确显示
- ✅ 主题切换功能正常

### 集成
- ✅ 前后端数据交互正确
- ✅ 完整用户流程可执行
- ✅ 数据持久化正常
- ✅ 实时反馈及时准确

---

## 📝 测试报告模板

测试完成后，填写以下报告:

```
LearnEnglish 测试报告
====================

测试日期: ___________
测试人员: ___________

后端测试:
□ 单词管理API     [通过/失败]
□ 进度追踪API     [通过/失败]
□ 练习系统API     [通过/失败]
□ 成就系统API     [通过/失败]
□ 间隔重复算法     [通过/失败]

前端测试:
□ 单词学习页      [通过/失败]
□ 练习测试页      [通过/失败]
□ 进度统计页      [通过/失败]
□ 设置页         [通过/失败]
□ 移动端响应式     [通过/失败]

集成测试:
□ 完整学习流程     [通过/失败]
□ 练习测试流程     [通过/失败]
□ 成就解锁流程     [通过/失败]

Docker部署:
□ 容器构建       [通过/失败]
□ 服务启动       [通过/失败]
□ API访问       [通过/失败]

问题记录:
1. ___________
2. ___________

总体评价: [优秀/良好/合格/不合格]
```

---

**测试完成后，应用即可发布使用！** 🎉
